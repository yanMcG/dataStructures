existing code recieved through blackboard has been developed on. majority of 
code for GraphPanel was given based on John B. Matthews. This system goes through 
a binary search tree alogrithim that can handle violations and go through the caes for each which are;
  case1: unlc ei s red --> recolour
  case2: left left so rotateSubTree right
  case3: right right which rotateSubTree left
